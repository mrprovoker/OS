# Наблюдатель

Для проверки можно запустить с приготовленным config'ом (должен лежать в /)  
Перемещение: ``` sudo mv config / ```


Пример запуска: 
```
gcc os5.c -o pr
./pr
```

Проверка:
```
cat /var/log/syslog
```